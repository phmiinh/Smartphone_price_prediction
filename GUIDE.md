Hướng dẫn build

1. Cài đặt
- UI next.js + react
pnpm setup
pnpm install
- Cài đặt python
cd model_service
pip install -r requirements.txt

2. Chạy song song cả 2 trên terminal khác nhau
- Chạy UI
pnpm dev
- Chạy service 
cd model_service
python main.y